name: Apply electricity source migration

on:
  push:
    branches:
      - audit/electricity-access
    paths:
      - ".github/workflows/apply-electricity-source-migration.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: audit/electricity-access

      - name: Apply audited source migration
        shell: bash
        run: |
          set -euo pipefail
          python .github/scripts/apply_electricity_source_migration.py
          python -m json.tool config/world-bank-indicators.json >/dev/null
          git diff --check
          git diff -- config/world-bank-indicators.json docs/statistics-source-policy.md

      - name: Commit migration
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add config/world-bank-indicators.json docs/statistics-source-policy.md
          git commit -m "Move electricity access to direct World Bank source"
          git push origin HEAD:audit/electricity-access
