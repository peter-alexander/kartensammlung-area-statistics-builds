name: Temporary WHO WDI health audit

on:
  push:
    branches:
      - audit/who-wdi-health

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pycountry

      - name: Compare direct WHO series with WDI
        run: python .github/scripts/tmp_compare_who_wdi.py
