name: Audit CRU-CY v4.10

on:
  push:
    branches:
      - audit/cru-cy-v410
    paths:
      - "config/cru-cy-v410-country-map.json"
      - "scripts/audit_cru_cy_v410.py"
      - "scripts/audit_cru_cy_v410_values.py"
      - ".github/workflows/audit-cru-cy-v410.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate audit scripts
        run: |
          set -euo pipefail
          python -m py_compile scripts/audit_cru_cy_v410.py scripts/audit_cru_cy_v410_values.py
          python -m json.tool config/cru-cy-v410-country-map.json >/dev/null
          git diff --check

      - name: Audit CRU-CY v4.10 structure
        run: |
          set -o pipefail
          mkdir -p diagnostics
          python scripts/audit_cru_cy_v410.py 2>&1 | tee diagnostics/cru-cy-v410-audit.log

      - name: Audit mapped CRU-CY v4.10 values
        run: |
          set -o pipefail
          python scripts/audit_cru_cy_v410_values.py 2>&1 | tee diagnostics/cru-cy-v410-values-audit.log

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cru-cy-v410-audit-${{ github.sha }}
          path: diagnostics/
          if-no-files-found: error
          retention-days: 14
